def LinearSearch(arr,value):
    for i in range(len(arr)):
        if arr[i]==value:
            return 'element found at index -->',arr.index(value)
    return 'not found' 
arr=[20,40,30,50,90] 
print(LinearSearch(arr,90))
    
